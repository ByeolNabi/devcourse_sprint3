"use strict";
// 변수에 데이터 타입 명시하기
let stdId = 1111;
let stdName = "lee";
let age = 10;
let completed = false;
// 아래와 같은 타입들을 가지고 있는 object를 리턴하라
function getInfo(id) {
    return null;
}
// 함수의 데이터 타입 명시 (매개변수, 리턴타입)
function Plus(a, b) {
    return a + b;
}
